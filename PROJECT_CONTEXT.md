# ניהול הבית - הקשר הפרויקט

קובץ זה מתעד את הפרויקט עבור המשך עבודה בצ'אטים עתידיים.

## הקבצים
- `index.html` - האפליקציה המלאה (single-file)
- `shabat.png` - רקע למצב שבת
- `hag.png` - רקע למצב חג

## עקרונות
- HTML יחיד עם CSS+JS פנימי
- localStorage עם prefix `beithaim_`
- **אופליין מלא**: @hebcal/core@5.10.1 מוטמע inline (לא CDN); פונטים Heebo+Frank Ruhl Libre מוטמעים base64 (עברית+לטינית, כל המשקלים). אפס תלות רשת.
- RTL, עברית, פלטה רשמית (navy/zהב/בורגונדי)
- מותאם לטאבלט 1024×768 (יעד: iPad, הוספה למסך הבית)
- בית שמש לזמני היום

## היסטוריית שינויים
- גרסה אונליין מקורית שמורה ב-`index.online-backup.html`.
- תוקן: `HDate.monthName` (סטטי, הוסר ב-hebcal v5) → `HDate.getMonthName` ב-14 מקומות. באג סמוי שהיה שובר גם עם ה-CDN.

## אחסון ופריסה
- מתארח ב-GitHub Pages: https://shimik2545.github.io/kaplan-family/ (מאגר shimik2545/kaplan-family).
- קבצים אישיים (.ged/.xlsx) מוחרגים דרך .gitignore ממאגר ציבורי.

## שלב 2: סנכרון Supabase [הושלם 2026-07-24]
אופליין-פירסט עם סנכרון אופציונלי. מטרה: גיבוי בענן + עדכון מרחוק.
- Supabase project: azfazetorfokwyybdkof. טבלה `household_state` (user_id uuid PK, data jsonb, updated_at). RLS לפי auth.uid().
- אימות: אימייל+סיסמה (Supabase Auth), אישור-מייל כבוי. anon key מוטמע (בטוח — RLS מגן).
- ספריית supabase-js מוטמעת inline. מודל: מסמך State יחיד למשתמש, last-write-wins לפי חותמת זמן.
- זרימה: התחברות→סנכרון; saveState→דחיפה מושהית (2.5ש); אתחול+online→סנכרון. גיבוי מקומי לפני משיכה דורסת.
- אומת מקצה-לקצה (push/pull/RLS). המשתמש חיבר חשבון אמיתי והנתונים בענן.
- באג קריטי שתוקן: ענן ריק היה דורס נתונים מקומיים בהתחברות ראשונה. תוקן — כלל ברזל: נתונים מקומיים לעולם לא נדרסים ע"י ענן ריק. שוחזר מגיבוי.
- נותר: למחוק משתמש-בדיקה synctest.deleteme@example.com מלוח הבקרה של Supabase.

## PWA — פתיחה כאפליקציה [2026-07-24]
- תגיות apple-mobile-web-app (standalone, כותרת, שורת סטטוס black) + theme-color #0b1220.
- manifest.json (display standalone, RTL, אייקונים). אייקונים: icon-192/512.png, apple-touch-icon.png (בית זהב על נייבי, נוצרו ב-PIL).
- Service Worker (sw.js, cache beithaim-v1): רשת-תחילה למסמך, מטמון-תחילה לתמונות/אייקונים, Supabase תמיד רשת (לא נשמר). עובד רק ב-https (לא file://).
- אומת חי: SW activated, 8 קבצים במטמון, manifest תקין. באייפד: הוסף למסך הבית → נפתח במסך מלא.

## באנר "יצאה גרסה חדשה — רענן עכשיו" [2026-07-24]
מזהה משתמש שתקוע על bundle ישן (טאב פתוח ימים) ומזמין אותו לרענן. אין כפתור סגירה — הדרך היחידה להעלים את הרצועה היא לרענן.
- **קבוע-גרסה**: `const APP_VERSION` (נוצר; לא היה קודם) בראש הסקריפט, ליד CONFIG. מודבק ל-bundle.
- **"נקודת-קצה"**: קובץ סטטי `version.json` בשורש (`{version, forced}`) — מחליף את endpoint ה-Next.js של מימוש-הייחוס, כי GitHub Pages סטטי. הלקוח מושך אותו כל 30ש' + בכל `visibilitychange`, מדלג כשהטאב מוסתר. cache-busting (`?t=`) + `cache:no-store` מנטרלים מטמון-דפדפן ו-CDN של GitHub (המקבילה ל-no-store+force-dynamic).
- **SW**: `version.json` הוחרג מהמטמון (תמיד רשת), אחרת cache-first היה מקפיא את הגרסה — הבאנר לא היה מופיע לאף אחד.
- **התנהגות**: לחיצה → הכפתור מתחלף בספינר יחיד (האייקון-בעיגול סטטי), דגל ב-sessionStorage, `location.reload()` אחרי 200ms. אחרי הטעינה — רצועה ירוקה "המערכת עודכנה" 4.5ש'. רענון רק בלחיצה, לעולם לא אוטומטי.
- **דחיפה-ידנית**: `forced:true` ב-version.json מציג לכולם גם כשמעודכנים (מקבילה גלובלית ל-force_update_banner הפר-משתמש; אין מסך ניהול-משתמשים כאן). אישור מקומי ל-session אחרי רענון.
- **תצוגה-מקדימה**: `?previewVersionBanner=1` מכריח את הבאנר. נושא יחיד (האפליקציה כולה כהה — אין אזור עם שפה-עיצובית נפרדת). עיצוב בפלטת האפליקציה (זהב/נייבי, ירוק-הצלחה), אנימציות CSS גלובליות + prefers-reduced-motion. אייקונים inline (refresh, checkCircle), לא אימוג'ים.
- **בפריסה חדשה**: להעלות את המספר גם ב-`APP_VERSION` (index.html) וגם ב-`version.json`.
- אומת: preview מציג רצועה עם "רענן עכשיו" ואפס כפתורי-סגירה; לחיצה→ספינר יחיד→דגל→רענון→רצועה ירוקה; זיהוי-מיושן אמיתי (V01.00.00≠V01.00.01) מציג באנר; forced מציג גם כשמעודכן; מעודכן+לא-forced=שקט; דילוג-בדיקה כשהטאב מוסתר (visibilityState=hidden).

## שיטת צאת השבת/החג — רגיל / רבנו תם [2026-07-25]
בהגדרות בורר "שיטת צאת השבת/החג": **רגיל** (צאת הכוכבים 8.5° + 12 דק') או **רבנו תם** (72 דק' אחרי השקיעה). נשמר ב-State.settings.shabbatExitMethod. מקור-אמת יחיד: `shabbatExitDate(z)` — כל התצוגות (שעון, לוח, מסך תאריך) וגם מעבר-מצב-שבת (isShabbatNow) עוברות דרכו. ברירת מחדל 'standard'.

## אירוח שבתות — מי מתארח אצלנו [2026-07-25]
לכל שבת אפשר לרשום מי מתארח אצלנו. נתונים: State.shabbatGuests (מערך רשומות `{date:'YYYY-MM-DD', guests:[{kind:'family'|'member'|'text', id?, name}], note}`), מסונכרן ענן כמו כל State.
- **מסך ייעודי "אירוח שבתות"** (view id `hosting`, בתפריט): 14 השבתות הקרובות עם פרשה + תאריך + צ'יפים של המתארחים; לחיצה פותחת עורך.
- **מהלוח**: מסך פרטי-תאריך של שבת מציג סעיף "מתארחים אצלנו" + כפתור עריכה, ובתפריט ההוספה יש "מתארחים בשבת". תא-שבת בלוח מציג "N מתארחים".
- **עורך** (`manageShabbatGuests(dateStr)`): צ'יפים עם הסרה; הוספה מיחידות-משפחה (State.families), מבני-משפחה (State.family), או טקסט חופשי; הערה חופשית. בחירה מהמאגר + חופשי (החלטת המשתמש). כל השבת יחד (בלי סעודות), רק כיוון "מתארח אצלנו". פתוח לכולם (לא requireAdmin — כמו רשימות שבת).
- אומת בדפדפן: שמירה/טעינה, הוספה/הסרה חיה, dedupe (משפחה שכבר נבחרה מוסתרת), מסך ייעודי (14 שורות), אינטגרציית לוח, פרשה בכותרת. נוסף אייקון `user` (יחיד).

## סקירת מערכת מלאה + תיקונים והקשחה [2026-07-24]
נבדקו כל 20 המסכים בקוד ובדפדפן (לחיצה על כל כפתור). תוקן:
- **יציאת שבת**: isShabbatNow יצא ב-tzeit+1 דקה בעוד התצוגה הראתה tzeit+12 — יושר ל-tzeit+12 בכל 3 המקומות.
- **esc()** (ליד toast): כל טקסט-משתמש ב-value/textarea/option/רשימות עבר escaping. מרכאות בשם היו קוטעות עריכה ומוחקות טקסט. highlightMatch גם מבצע esc.
- **הגנת מנהל**: requireAdmin(retryFn) + סיסמה (ברירת מחדל **7391**, State.settings.adminPin, שינוי בהגדרות). חוסם: כניסה להגדרות (בnavigate) + כל פונקציות המחיקה/איפוס/seed (20 פונקציות). פתוח 5 דקות אחרי הזנה (RealDate — לא מושפע מסימולציית זמן). זו הגנה משפחתית client-side, לא אבטחה אמיתית.
- **טאבלט**: כפתורי עריכה/מחיקה של יחידת-משפחה היו hover-only — נוספו @media (hover:none) + כפתור "מחק משפחה" במודל פרטי-משפחה.
- **טיולים**: טיול נחשב "עבר" רק בסוף היום (היה: ברגע שעת היציאה עברה — הרשימה ננעלה בבוקר הטיול).
- **פרשה כפולה** במסך שבת ("פרשת פָּרָשַׁת"): ה-strip של getParsha לא התחשב בניקוד — תוקן regex.
- **מחיקת בן משפחה** מנקה עכשיו גם יארצייט משויך (familyId), לא רק יום-הולדת.
- הוסרו אימוג'ים (toast שמירה, אייקון התראה).
נבדק ואומת תקין: כל זרימות הוספה/עריכה/מחיקה, חישובי עברי (אדר מעובר, ל' חשון, גיל, יארצייט), כניסת/יציאת שבת בסימולציה, כספים, סנכרון ענן (סטטי).

## Views קיימים
home, calendar, zmanim, family, birthdays, yahrzeits, events, tasks, messages, parents, shabbat (רשימות), halacha, finance, loans, trips, ravkav (בקרוב), brachot, datelookup, settings

## פיצ'רים מרכזיים
- שעון אנלוגי + דיגיטלי
- לוח שנה עברי-לועזי
- 13 זמני יום (בית שמש)
- מצב שבת/חג עם רקע, ברכות באידיש (גוט שאבעס / א פריילאכן יום טוב)
- טיימר אדום 10 דק' לפני שקיעה + הכרזות קוליות
- סימולציית זמן
- ניהול בני משפחה + יחידות משפחה + קישורים
- חיפוש בני משפחה
- ימי הולדת לפי תאריך עברי + כרטיס מזל טוב + קונפטי
- יארצייט עם המרה לעברי
- 200+ ברכות
- ניהול כספים משפחתי

## להמשיך לעבוד
תפעיל את הסקיל **`beit-management`** והוא יטעין את כל ההקשר. או תאמר משפט כמו "אני רוצה להמשיך לעבוד על ניהול הבית".
